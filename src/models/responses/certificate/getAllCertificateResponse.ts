export interface Certificate {
  id: string;
  studentId: string;
  url: string;
  name: string;
}

export interface getAllCertificateResponse {
  id: string;
  studentId: string;
  url: string;
  name: string;
}
